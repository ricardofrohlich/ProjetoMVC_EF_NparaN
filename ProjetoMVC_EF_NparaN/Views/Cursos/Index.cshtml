@model IEnumerable<ProjetoMVC_EF_NparaN.Models.Curso>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NomeCurso)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomeCurso)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CursoID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CursoID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CursoID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
